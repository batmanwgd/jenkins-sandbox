pipeline {

    agent any
    
    stages {
        stage('build') {
            steps {
                sh 'docker ps'
                sh 'ls -las'
            }
        }
    }
}

// vim: set ft=groovy :  

